#include<bits/stdc++.h>
using namespace std;
#define lint long long int
#define loop(n) for(lint i=0;i<n;i++)
int main() 
{
    int t;  cin>>t;
    while(t--)
    {
        lint size;   cin>>size;
        // int arr[size];
        // vector<int>v(size);
        // unordered_map <int,int> m;
        cout<<endl;        
    }       
}

